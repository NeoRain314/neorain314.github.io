# Static Presentation PDFs

Drop static PDF files for mock presentation data in this folder.

The first mock presentation expects this file:

- `test-presentation.pdf`

In the running Vite app, that file is available at:

- `/presentations/test-presentation.pdf`

After adding or replacing the PDF, refresh the browser so the mock presentation preview and VR slides are regenerated.
