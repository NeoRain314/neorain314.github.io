# Brand Assets

Put the custom logo image in this folder with this exact filename:

- `logo.png`

The header loads it from:

- `/brand/logo.png`

After adding or replacing the image, refresh the browser.
